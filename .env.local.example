# Supabase Configuration
# After running `npx supabase start`, copy the API URL and anon key here
VITE_SUPABASE_URL=http://127.0.0.1:54621
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# Database Schema
# Set to 'niwalog' to use the niwalog schema, or 'public' (default) for the public schema
# Note: When using 'niwalog', you must also add 'niwalog' to the Exposed schemas
#       in Supabase Dashboard: Project Settings → API → Exposed schemas
VITE_DB_SCHEMA=niwalog
